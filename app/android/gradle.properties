org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# CI build performance: compile plugin subprojects in parallel, disable daemon
# (ephemeral runner has no reuse benefit), enable local build cache.
# NOTE: configuration-cache is intentionally omitted — incompatible with
# dev.flutter.flutter-gradle-plugin at this AGP version.
org.gradle.parallel=true
org.gradle.daemon=false
org.gradle.caching=true
