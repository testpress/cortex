/// Common UI constants for the Store package.
class StoreConstants {
  StoreConstants._();

  /// Standard width for horizontal scrolling cards.
  static const double cardWidth = 240.0;

  /// Header letter spacing for section titles.
  static const double sectionHeaderLetterSpacing = 1.1;

  /// Default aspect ratio for card thumbnails.
  static const double cardAspectRatio = 16 / 9;
}
