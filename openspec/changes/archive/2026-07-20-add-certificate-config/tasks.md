## 1. Implement Configuration Flag

- [x] 1.1 Add `showCertificate` boolean flag to `AppConfig` using `bool.fromEnvironment` with default `false`.
- [x] 1.2 Conditionally render Certificate settings based on the flag in `paid_active_account_preferences_section.dart`.
- [x] 1.3 Add flag to `config/elearn_brilliantpala.json`.
- [x] 1.4 Add flag to `config/rays.json`.
