# Tasks

- [x] Identify screens with nested `AppShell` usage.
- [x] Replace `AppShell` with `Container` in `ChapterDetailPage`.
- [x] Replace `AppShell` with `Container` in `ReviewAnswerDetailScreen`.
- [x] Replace `AppShell` with `Container` in `PaidActiveProfileScreen`.
- [x] Verify layout alignment in both Portrait (Tab Bar) and Landscape (Navigation Rail) modes.
