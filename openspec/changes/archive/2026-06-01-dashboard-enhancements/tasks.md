## 1. Hero Banner Interactivity

- [x] 1.1 Wrap `PageView.builder` in `packages/courses/lib/widgets/hero_banner_carousel.dart` with a `Listener`.
- [x] 1.2 Implement `onPointerDown`, `onPointerUp`, and `onPointerCancel` to pause and resume `_timer`.

## 2. Horizontal Carousel Scrolling

- [x] 2.1 Update `packages/core/lib/widgets/app_carousel.dart` to set `pageSnapping: false` on its `PageView.builder`.

## 3. Recently Completed List Styling

- [x] 3.1 Modify `packages/testpress/lib/screens/dashboard/widgets/lesson_cards_section.dart` to remove the 0.6 opacity effect from `LessonCardWidget` when `isCompleted` is true.
