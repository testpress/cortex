## 1. API Update

- [x] 1.1 Update `getPosts` in `packages/core/lib/data/sources/http_data_source.dart` to include `ordering=-published_date` as a query parameter.

## 2. Verification

- [x] 2.1 Verify that the API correctly returns posts ordered by published date in descending order.