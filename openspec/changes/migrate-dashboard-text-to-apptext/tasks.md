## 1. Refactor Dashboard Text Elements

- [x] 1.1 Replace category section headers in `lesson_cards_section.dart` with `AppText.title`
- [x] 1.2 Replace lesson card titles in `lesson_cards_section.dart` with `AppText.cardTitle`
- [x] 1.3 Replace lesson card chapter names in `lesson_cards_section.dart` with `AppText.cardSubtitle`
- [x] 1.4 Replace lesson card metadata duration labels in `lesson_cards_section.dart` with `AppText.cardCaption`
- [x] 1.5 Replace lesson card progress percentage text in `lesson_cards_section.dart` with `AppText.labelSmall`

## 2. Refactor Remaining Text Elements

- [x] 2.1 Replace bookmarks generic error text in `bookmarks_screen.dart` with `AppText.body`
- [x] 2.2 Replace SnackBar and video thumbnail text in `downloads_screen.dart` with `AppText` semantic variants
- [x] 2.3 Replace button text label in `ask_doubt_fab.dart` with `AppText.labelBold`
- [x] 2.4 Replace text items in `attachment_viewer.dart` with `AppText` semantic variants
- [x] 2.5 Replace video quality setting spans in `app_settings_screen.dart` with `AppText.labelSmall`

## 3. Verification

- [x] 3.1 Verify packages compile and run successfully
- [x] 3.2 Run formatting and analyzer checks on modified packages
- [x] 3.3 Visually verify rendering of modified components
