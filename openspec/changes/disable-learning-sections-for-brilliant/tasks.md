## 1. Implementation

- [x] 1.1 Modify `packages/testpress/lib/screens/dashboard/paid_active_home_screen.dart` to conditionally exclude the Learning Performance (`studyMomentum`) widget for Brilliant Pala.
- [x] 1.2 Modify `packages/profile/lib/screens/paid_active_profile_screen.dart` to conditionally exclude `ProfileLearningSnapshot` (learning at a glance), `EnrolledCoursesSection` (active courses), and `RecentActivitySection` (recent learning) for Brilliant Pala.

## 2. Verification

- [ ] 2.1 Verify that the app builds correctly.
- [ ] 2.2 Verify that the features are hidden when configured for Brilliant Pala.
