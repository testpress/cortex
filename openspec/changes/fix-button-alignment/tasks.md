## 1. Profile Package Implementation

- [x] 1.1 Set `fullWidth: true` for action buttons in `CertificatePreviewScreen` (`packages/profile/lib/screens/certificate_preview_screen.dart`)
- [x] 1.2 Set `fullWidth: true` for the "View Certificate" button in `_UnlockedCertificateDetails` within `CertificatesScreen` (`packages/profile/lib/screens/certificates_screen.dart`)

## 2. Courses Package Implementation

- [x] 2.1 Set `fullWidth: true` for "Previous" and "Next" buttons in `LessonNavigationFooter` (`packages/courses/lib/widgets/lesson_detail/lesson_navigation_footer.dart`)
